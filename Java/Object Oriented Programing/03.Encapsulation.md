# Encapsulation
Grouping of data members and corressponding method and behebiours into a single unit is know as encapsulation.

Advantages:
- Security
- Enhancement without effecting end use.
- Maintinibiliy and modularity will be improved

## Tightly encapsulated class
If every data members are declared as `private` it's know as tightly encapsulated class.