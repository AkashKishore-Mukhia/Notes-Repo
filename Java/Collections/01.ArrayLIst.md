# ArrayList

- ArrayList and vector classes implements random access interface, so that any random element we can access with the same speed.
- If our requirement is fast random access we should got with ArrayList



Note: Random access interface present in java.util package, and it does't contain any method it's a marker interface.