
## What is a Shell:
A program that interprets the commands you type in your terminal and passes them on to the operating system.


change default shell to bash: 
```sh
$ chsh -s <path-of-shell(e.g./bin/bash)>
```

