# Encapsulation
Grouping of data members and corressponding method and behaviour into a single unit is know as encapsulation.

Advantages:
- Security
- Enhancement without effecting end use.
- Maintainability and modularity will be improved

## Tightly encapsulated class
If every data members are declared as `private` it's know as tightly encapsulated class.